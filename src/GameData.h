#ifndef __CacheWars__GameData__
#define __CacheWars__GameData__

#include <ofUtils.h>

struct ShipData {
    ofPoint position;
};

struct EngineData {
    float direction;
    float speed;
};

struct RenderData {
    ofColor color;
};

namespace GameData {

    
    
}

#endif
